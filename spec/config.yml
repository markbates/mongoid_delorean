test:
  sessions:
    default:
      database: mongoid_delorean_test
      hosts:
        - localhost:27017
      options:
        safe: true
        consistency: :strong
        max_retries: 1
        retry_interval: 0
